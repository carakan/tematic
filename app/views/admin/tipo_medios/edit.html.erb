<h1>Editar  tipo_medio</h1>

<%= error_messages_for :tipo_medio %>

<% form_for(@tipo_medio, :url => admin_tipo_medio_path) do |f| %>
  <p>
    <b>Tipomedio</b><br />
    <%= f.text_field :tipoMedio %>
  </p>

  <p>
    <%= f.submit "Actualizar" %>
  </p>
<% end %>

<%= link_to 'Mostrar', admin_tipo_medio_path(@tipo_medio) %> |
<%= link_to 'Ir al listado',admin_tipo_medios_path %>
